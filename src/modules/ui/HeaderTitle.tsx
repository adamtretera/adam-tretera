export function HeaderTitle({ title }) {
    return (
        <header>
            <h1 className="flex justify-center  py-4 pt-24  text-4xl sm:pt-32	sm:text-6xl">{title}</h1>
        </header>
    );
}
